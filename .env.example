# This project does not require secrets for the current offline browser build.
# These placeholders exist so future contributors have a standard config entry point.

APP_NAME=Local PDF Toolkit
APP_ENV=development
